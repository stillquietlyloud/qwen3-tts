# ── Core ML stack ────────────────────────────────────────────────────────────
# PyTorch – install the CUDA 12.1 wheel from the PyTorch index (see setup.sh)
# torch>=2.3.0  (pinned via setup.sh / Docker build)

# Qwen3-TTS Python library (installs transformers, tokenizers, soundfile, etc.)
qwen-tts>=0.1.0

# ── API server ────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# ── Audio encoding ────────────────────────────────────────────────────────────
soundfile>=0.12.1
# Optional: MP3 output (requires ffmpeg system package)
# pydub>=0.25.1

# ── Performance / reliability ─────────────────────────────────────────────────
# FlashAttention 2 – installed separately (needs CUDA headers); see setup.sh
# flash-attn>=2.5.0  # do NOT put here – install with --no-build-isolation

# ── Optional utilities ────────────────────────────────────────────────────────
python-multipart>=0.0.9  # for future multipart/form-data uploads
httpx>=0.27.0            # for async HTTP calls from tests / clients
